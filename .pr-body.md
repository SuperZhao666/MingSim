## 目标

修复深度审计 Wave 2A + 2B 三项 P1（DECREE-TRUSTED-AUTHORIZATION + DECREE-SHIPMENT-LINK-INVARIANT）：

### P1-AUTH-01：授权策略注入 —— 调用方不能再提供审核策略
- `CreateDecreeCommand` **移除 `RequiredCapability` / `RequiredResourceId`**：命令只表达业务意图（DecreeKind），
  任何调用方试图指定能力/资源域作为审核策略的旧写法在编译期即失败（"编译器红"已实证：移除字段后
  SmokeTests/SoakTests 旧调用点全部 CS1729，见下文验证）。
- 内核按 `DecreeKind` 内置 **trusted 映射**（`ResolveDecreePolicy`，Runtime 私有单一来源）决定承办能力与资源域：
  General/RationReduction/ExpediteSupply(催饷) → PlanLogistics；AllocateSupply(拨饷) → AllocateFinance；
  RequestSupply(请饷) → 请愿文书（无承办能力要求、创建不扣预算、进入 Submitted，批准后才转可执行并扣预算）。
  资源域固定为 null（任意辖区）：政令不绑定具体路线（DESIGN），调用方不可覆盖。
- 请饷最小 request 状态：复用既有 `DecreeStatus.Submitted`，新增最小 `ApproveDecreeCommand`
  （Submitted → Executing + 扣除批准预算 + DecreeApproved 审计事件）；重复批准/批准非待批准政令结构化拒绝。

### P1-AUTH-02：越权/伪造签发人 —— 内核校验 issuer
- 旧实现只校验承办人存在，签发人（ActorId）可被任意伪造。现在签发人必须是世界内真实角色，
  否则 `DECREE_ISSUER_UNAUTHORIZED`（接纳与批准两条路径都校验）。

### P1-DECREE-03：LinkedShipment 不变量
- 接纳时绑定运输单必须存在且为 Planned/InTransit：不存在 → `DECREE_SHIPMENT_NOT_FOUND`；
  已抵达 → `DECREE_SHIPMENT_ALREADY_ARRIVED`；同一运输单最多被一个 active（Executing/Submitted）政令占用 →
  `DECREE_SHIPMENT_ALREADY_BOUND`。
- 抵达时全部匹配政令按政令编号稳定顺序完成（原 FirstOrDefault 改为 foreach 稳定序）。
- 既有"先政令后运输单"用法必须改为"先运输单后政令"（SmokeTests/SoakTests 已按契约重排）。

## 修改路径

| 文件 | 用途 |
| --- | --- |
| `src/Ming.Simulation/Realtime/NingyuanP0Commands.cs` | DecreeKind 增加 ExpediteSupply/AllocateSupply/RequestSupply；CreateDecreeCommand 移除能力字段；新增 ApproveDecreeCommand |
| `src/Ming.Simulation/Realtime/RealtimeSimulationRuntime.cs` | ApplyDecree 重写（issuer 校验/绑定不变量/trusted 承办能力/请饷预算与状态）；ApplyApproveDecree；抵达完成稳定序；Fingerprint 适配 |
| `src/Ming.Domain/Decrees/DecreeState.cs` | 状态机最小扩展：initialStatus 参数（Submitted）＋ internal Approve()；RequiredCapability 改注为"内核写入的审计记录" |
| `src/Ming.Application/Commands/CommandFacade.cs` | EnqueueCreateDecree 签名适配（能力参数换 Kind）；新增 EnqueueApproveDecree |
| `src/Ming.Godot/scripts/DecreePanel.cs` | 只发 DecreeKind（模板 kind 催饷/拨饷/请饷 → DecreeKind 映射），不再传 capability |
| `tests/Ming.SmokeTests/Program.cs` | 新增 4 条验收（伪造策略编译期契约＋越权签发人、请饷不扣预算→批准才扣、绑定不变量三类错误码、两政令同单按契约完成）；CreateDecree helper 移除能力参数；绑定测试按新契约重排；场景世界 works 授权改为任意辖区（对齐"政令承办策略资源域=任意辖区"契约） |
| `tests/Ming.SoakTests/NingyuanSeedReplay.cs`、`StrategyExplorer.cs` | 编译适配（能力参数移除）；长跑脚本按绑定不变量重排（先运输单后政令），减耗令改显式 Kind |

## 先写失败测试（测试先行证据）

1. **编译器红**：移除字段后，SmokeTests/SoakTests 旧调用点（携带 RequiredCapability/RequiredResourceId）全部 CS1729 编译失败
   （构建日志实证），证明"伪造 RequiredCapability 降级"入口已被移除；新测试用反射断言命令类型不再暴露这两个属性。
2. **越权 issuer**：签发人 `ghost`（不存在）→ `DECREE_ISSUER_UNAUTHORIZED`，拒绝不改变世界。
3. **策略不可降级**：拨饷令（AllocateSupply）承办人只持 PlanLogistics → `DECREE_RESPONSIBLE_UNAUTHORIZED`。
4. **请饷不扣预算**：RequestSupply 创建 → Submitted、SpentSilver=0；批准 → SpentSilver=2000、Executing、DecreeApproved；
   再次批准（新 CommandId）→ `DECREE_NOT_PENDING_APPROVAL` 且不二次扣预算。
5. **绑定不变量**：不存在/已抵达/已占用运输单绑定 → 三个明确错误码，拒绝不创建状态不扣预算。
6. **两政令同单**：第二道绑定被 `DECREE_SHIPMENT_ALREADY_BOUND` 拒绝；唯一被接纳政令在抵达时按稳定顺序完成一次。
7. **负向实证**：临时禁用签发人校验后 `ShouldForbidForgedDecreePolicyAndFakeIssuer` 立即失败
   （"不存在的签发人必须被拒绝 DECREE_ISSUER_UNAUTHORIZED"），恢复后全绿——测试确实守卫该修复。

## 实际验证（全部重跑）

- 构建：`dotnet build MyGame.sln -m:1 -p:NuGetAudit=false -c Release --nologo` → **0 个警告 0 个错误**。
- `dotnet run --project tests/Ming.SmokeTests -c Release --no-build` → "MingSim 实时内核补审测试全部通过。"（含 4 条新验收）
- `dotnet run --project tests/Ming.SoakTests -c Release --no-build` → "MingSim 长跑测试全部通过。"（含重排后的 NingyuanSeedReplay 90 日重放与 StrategyExplorer 四策略探索）
- `dotnet run --project tests/Ming.Agents.ContractTests -c Release --no-build` → "Ming.Agents OpenAI-compatible contract tests passed."
- Godot headless（真实 1629 场景 + 默认催饷模板）：
  `m3_decree_endgame_acceptance.gd` → **M3_DECREE_ENDGAME_ACCEPTANCE: PASS**
  （催饷令模板经 Kind 映射 ExpediteSupply 被内核接纳并显示"执行中"；六维终局复盘不回归）。
  ​`guide_acceptance.gd` → PASS；`main_ui_transition_acceptance.gd` → PASS；`map_fleet_acceptance.gd` → PASS；`map_input_acceptance.gd` → PASS。
- Godot C# 装配说明：headless 运行需要先 `dotnet build src/Ming.Godot/Ming.Godot.csproj -c Debug`（Godot 默认加载 Debug 装配，仓库 .godot/ 不入库），并先执行一次 `--headless --editor --path . --quit` 导入资产。



## 独立审查 REQUEST_CHANGES 修复（本轮）

### P1-A（阻断）：批准人无能力授权 —— 已修复
- **根因**：ApproveDecreeCommand 只校验"批准人是真实角色"，任意真实角色可"签发请饷→自我批准"两步耗尽国库。
- **修复**：批准人必须经 CapabilityAuthorizer 持有 **AllocateFinance（财权）**（角色不存在时同样被拒），
  否则 `DECREE_APPROVER_UNAUTHORIZED` 且国库/政令状态不变。请饷批准是动用中央预算的财政决定，授权落在财权上；
  真实场景中由户部槽位（hubu-slot，AllocateFinance）批准。"皇帝"分支未实现：领域无君主角色标记
  （皇帝/崇祯与测试角色都无任何 CapabilityGrant），最小实现取审查结论的"持 AllocateFinance 的角色"路径。
- **被批准政令 Kind 校验**：只有请愿文书（RequestSupply）进入 Submitted（内核创建时不变量），
  因此 `Status==Submitted` 即等价于 `Kind==RequestSupply`（DecreeKind 定义在 Simulation，
  Domain 的 DecreeState 不存储 Kind——若存储将产生 Domain→Simulation 非法依赖；状态机不变量是唯一且充分的校验）。
- **补测试**：`ShouldRejectUnauthorizedApproverAndKeepTreasury`——war（真实角色无财权）与 ghost（不存在）批准均被拒
  （DECREE_APPROVER_UNAUTHORIZED）、国库 0 不变、政令保持 Submitted。负向实证：临时禁用批准人校验后该测试立即失败，恢复后全绿。

### P1-C（阻断）：快照版本门禁未随命令指纹形状变更升级 —— 已修复
- **根因**：CreateDecreeCommand 指纹移除 RequiredCapability/RequiredResourceId + 新增 ApproveDecreeCommand 分支，
  但 RealtimeSnapshotSchema.Version 仍为 7——v7 快照含 pending 政令命令时恢复会哈希失配而非干净版本拒绝。
- **修复**：`RealtimeSnapshotSchema.Version` 7→8（注释记录 6→7→8 门禁沿革）。
- **补验收**：`ShouldRejectV7SnapshotWithPendingDecreeCommands`——含 pending 政令命令的快照篡改 schema=7，
  恢复被版本门禁显式拒绝（"不支持实时快照版本"，fail-closed），而非哈希失配。

### P2 顺手修
- ① 批准路径复查绑定不变量：批准时若绑定运输单已抵达 → `DECREE_SHIPMENT_ALREADY_ARRIVED` 拒绝
  （避免"先抵后批滞留死锁"：抵达完成只处理 Executing 政令，Submitted 请饷不会自动完成）。
  测试：`ShouldRejectApprovalWhenBoundShipmentAlreadyArrived`。
- ② ApproveDecreeCommand 校验被批准政令为请愿文书：见 P1-A 的 `Status==Submitted ⟺ Kind==RequestSupply` 说明。
- ③ ApplyDecree 的 INVALID_OBJECT_ID 改走 RejectDecree（每次拒绝都留下 DecreeRejected 审计事件对；
  RejectDecree 事件载荷对 null DecreeId 加了占位符防崩溃）。

### 其余 P2 记录（非阻断，未实现）
- CommandFailureText 未收录新错误码（DECREE_APPROVER_UNAUTHORIZED 等）：该文件在允许修改清单外，
  面板对未收录码原样显示"未收录"，不掩盖。
- Submitted 请饷不过期（Expire 只处理 Executing）：未批准请愿不甩责（语义成立），超期自动归档留给后续任务。
- 拨饷模板默认承办机构 duliaoxiang-slot 会被内核 AllocateFinance 门禁正确拒绝：面板提示已写明需按文书种类选机构。

## 非目标 / 边界

- 未改 world.json、未给任何角色追加 CapabilityGrant（场景世界 works 的 PlanLogistics 由路线作用域改为任意辖区，
  是对齐"政令承办策略资源域=任意辖区"契约的测试夹具维护，非新增授权）。
- 未碰 Persistence / Agents / CanonicalStateHasher / RealtimeReadModel（DecreeState 保留能力字段作内核审计记录，
  避免触碰哈希与只读投影）。
- SoakTests 两文件仅做命令签名编译适配与绑定顺序重排（命令字段移除的机械连带，不在"禁止修改"清单）。
- CommandFailureText 未扩展新错误码（超出允许文件清单；面板对未收录错误码原样显示"未收录"，不掩盖）。

## 风险 / OPEN（P2 债务）

- 请饷奏疏处于 Submitted 期间不会因期限到期而 Expire（ExpireOverdueDecrees 只处理 Executing）：
  未批准的请愿不甩责（语义成立），长期滞留的待批准奏疏会一直显示在面板——MVP 可接受，后续任务再做
  超期自动归档。
- 拨饷模板默认承办机构仍是 duliaoxiang-slot（仅持 PlanLogistics），在内核 AllocateFinance 门禁下会被正确拒绝；
  面板提示已写明"承办人/请饷人填角色编号：duliaoxiang-slot（持调粮能力）、hubu-slot（持财粮会计）"，
  玩家需按文书种类选择正确机构——这是门禁生效的表现，不是缺陷。
- 快照 schema 未升级：政令状态机新增 Submitted 初态与 Approve 转换复用既有 DecreeStatus 枚举与状态哈希，
  旧快照（Executing 初态语义）不受影响。
