# 总控并发审查记录 — 2026-08-16

> 本记录由 Principal Engineer（总控）在「先建工作树 → 并发派发子代理 → 独立审查 + Bug Hunt → 推送分支 PR（不合并）」流程中产出。
> 遵守 `AGENTS.md`：第一原则「不信任任何 Agent，包括自己」；SHA 绑定是对抗「未审代码入主线」的唯一防线。

## 1. 流程与工作树

| 角色 | 工作树 | 分支 | 基线 |
| --- | --- | --- | --- |
| 总控（本记录） | `_worktrees/principal-oversight` | `principal-oversight` | main=3d75bc2 |
| 子代理 A（文档） | `_worktrees/docs-gap-update` | `docs-update-gap-status-post45` | main=3d75bc2 |
| 子代理 B（测试） | `_worktrees/test-characterize` | `test-characterize-realtime-behavior` | main=3d75bc2 |

基线证据（main=3d75bc2，PR#45 合并后）：
- `dotnet build MyGame.sln` → 0 警告、0 错误。
- `dotnet run --project tests/Ming.SmokeTests` → 「MingSim 实时内核补审测试全部通过」exit 0。
- 本地 SQLite 验收默认关闭（`MingSimEnableSqliteStore=false`），由联网 CI 执行，符合 csproj 设计。

## 2. 派发与兜底

- 子代理 A（文档）：一次派发即完成，回报完整。
- 子代理 B（测试）：首次派发与一次重试均被策略层拒绝（未执行任何工具调用）；第三次以纯编码指令重试成功。总控未接管写码——子代理 B 的代码由子代理产出，总控仅做独立审查，符合「Self Review / Independent Review 不得由开发者本人担任」。

## 3. Architecture Contract 汇总

### A 路（文档）
- 目标：增量标注 `docs/设计蓝图/12-现状差距与实施顺序.md` 第 4、5 节，反映 PR#45 落地状态。
- 允许修改：仅该 1 个 md。
- 禁止修改：src/、tests/、其他文件；禁止 push/merge/删除。
- 验收：只改 1 文件；每条「已闭合」必有 file:line 来源；FACT/INFERENCE/DESIGN/OPEN 语义。

### B 路（测试）
- 目标：为实时行军邻接、同刻事件稳定顺序、暂停/速度补充特征测试。
- 允许修改：仅 `tests/Ming.SmokeTests/`。
- 禁止修改：src/、docs/、csproj（除非必要）。
- 验收：构建 0 错误；冒烟测试全过；新测试真正断言行为；无 src/ 改动。

## 4. Independent Review（SHA 绑定）

### A 路 — head SHA `5be796ab557e92c0face0c757a89013764ae9d6a`
- `git diff --stat`：1 file changed, 33 insertions(+), 0 deletions。原文 0 删除，历史保留。✅
- 边界：仅 `docs/设计蓝图/12-现状差距与实施顺序.md`。✅
- 来源核验（对抗伪造来源 = P1）：总控逐条核对子代理所引 file:line，全部属实：
  - `SqliteCommitStore.cs:28` 类签名四端口合一；`:178` `CommitAll`；`:394` `BeginTransaction(deferred:false)`；`:426` `Rollback()`；`:642-645` 三层 checksum 列。✅
  - `CanonicalStateHasher.cs:25` `SchemaVersion=6`；`:62` 写 hashSchemaVersion；`:66` GameTime.UtcTicks；`:67-68` WorldVersion/CommitId；`:69` randomState；`:122` nextCreationSequence。✅
  - `SnapshotCodec.cs:38` `FormatVersion=3`；`:39-40` v1/v2 兼容。✅
  - `Ming.Persistence.csproj:20` 已引用 `Ming.Simulation`。✅
- 语义：FACT/INFERENCE/DESIGN/OPEN 标注正确；OPEN/DESIGN 项诚实落账（人物关系/目标、地图控制权、内容/规则版本未建模）。✅
- 结论：**PASS**，无 P0/P1。

### B 路 — head SHA `55fd2e5160c1f28cb7c149651a189c9b914c7257`
- `git diff --stat`：1 file changed, 284 insertions(+), 0 deletions。`git diff --name-only` 仅 `tests/Ming.SmokeTests/Program.cs`；`grep -c "^src/"` = 0。✅
- 边界：未触碰 src/、csproj、其他测试文件；仅在 Program.cs 增量。✅
- 运行证据（总控独立复跑，非采信子代理自述）：
  - `dotnet build MyGame.sln` → 0 警告、0 错误。
  - `dotnet run --project tests/Ming.SmokeTests --no-build` → 「MingSim 实时内核补审测试全部通过」exit 0。
- 代码审查（4 个新测试 + 2 个世界夹具）：
  - `ShouldCharacterizeMarchAdjacencyCheck`：不相邻/不存在/自环三场景归入 `PROVINCE_NOT_ADJACENT` 且版本不变、无 MovementState、无事件；相邻场景接纳、版本+1、MovementState 字段齐全、ArmyArrival phase=1 priority=0、产生 ArmyMarchStarted。多条件断言，非空洞 pass。✅
  - `ShouldCharacterizeSameTimeEventOrdering`：两军同刻同 phase 同 priority，按 CreationSequence 稳定排序；EventId 反映入队顺序；推进后 ArmyArrived 按创建顺序产出、两军抵达、Movements 清除。✅
  - `ShouldCharacterizePauseHoldsDueEventsUntilResume`：暂停后 AdvanceTo 到期之后，时间/位置/在途全冻结、无 ArmyArrived；恢复后推进，被挂起事件按原到期结算、军队抵达。逻辑自洽。✅
  - `ShouldCharacterizeSpeedBoundariesAndScaling`：[0.25,5.0] 闭区间合法、区间外抛 ArgumentOutOfRangeException；speed=1/2/0.5 线性缩放（6/12/3 游戏小时）；快照往返速度保持、hash 一致、继续按 3 倍缩放。✅
- 架构边界：测试仅经公开 API（Runtime/Command/ReadModel）读写，未越界直改 WorldState，符合「UI/Agent 只读 ReadModel 并提交 Command」。✅
- 确定性：FixedUtc + 显式 GameTime，可重放。✅
- 结论：**PASS**，无 P0/P1。

## 5. Bug Hunt

- A 路（纯文档，无代码攻击面）：攻击目标改为协议自洽性与来源一致性。未发现虚构来源；FACT 与 OPEN 分界清晰；标题层级连续无断裂。判定 **BUG_HUNT_CLEAR**。
- B 路：尝试寻找边界漏洞/吞异常/虚假绿。
  - 暂停测试最易「假绿」：已核验「暂停期 GameTime 冻结 + 恢复后推进 + 事件结算」三段独立断言，非单一 no-op 通过。
  - 速度测试用 `RequireThrows<ArgumentOutOfRangeException>` 显式断言抛异常，非吞掉。
  - 邻接测试对「不存在」与「不相邻」合并判定为刻画当前实现，已用注释标注，未来若拆分错误码需同步更新——属特征测试预期行为，非缺陷。
  - 未发现潜在失败场景。判定 **BUG_HUNT_CLEAR**。

## 6. P2 债务清单（落账，不阻塞）

- A-1：人物关系/目标未建模 → 哈希待补段并升 schema。
- A-2：动态地图控制权未建模 → 待「占领」玩法引入后补。
- A-3：内容/规则版本字段未引入 → 待 `world.json` 增版本号后纳入哈希。
- A-4：Snapshot 周期化/增量路径未单独实现 → 存档体积变大后再引入。
- A-5：外部 InputJournal 独立分表未引入 → 待 LLM Intent 成为独立流后再分。
- B-1：「不存在省份」与「不相邻」合并错误码为当前实现刻画，未来拆分需更新测试。
- B-2：速度区间 [0.25,5.0] 为设计锁定，若调整需更新测试。
- 既有未合并本地分支（feat/openai-compatible-provider、feat/ui-art-direction、fix/liaodong-evidence-review-findings、fix/realtime-kernel-review-findings、feat/m3-guide-hardening）含未合并提交，本轮未处理，建议后续单独评估其相对 main 的有效性与冲突。

## 7. 门禁汇总与处置

| 项 | A 路 | B 路 |
| --- | --- | --- |
| 编译通过 | N/A（纯文档） | ✅ 0 错误 |
| 自动测试通过 | N/A | ✅ 全过 |
| Architecture Contract 满足 | ✅ | ✅ |
| Independent Review PASS | ✅ SHA 5be796a | ✅ SHA 55fd2e5 |
| Bug Hunter 完成 | ✅ CLEAR | ✅ CLEAR |
| 无 P0/P1 | ✅ | ✅ |

**处置**：推送 A、B 两路分支与本审查记录分支至 origin，创建对应 Draft PR。**严格不执行 merge**（依用户指令与 AGENTS.md：合并须由独立 Merge Controller 在门禁满足后执行，本轮不合并）。

## 8. 高危指令审查

- 本轮未删除任何文件、分支、工作树；未执行 reset/force-push/改写历史。
- `git worktree add` 为新增操作；分支名为连字符形式（本机 git 2.55 拒绝带斜杠的新分支名，已规避）。
- 未触碰主仓库 `E:/20_Projects/MyGame` 的未提交改动（project.godot 等）。
