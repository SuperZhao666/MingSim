# 总控并发审查记录 — 2026-08-16（第二轮：多模态 UI 美术）

> 流程：总控用 ImageGen 生成美术资产 → 并发派发子代理各建工作树接线 → 总控独立审查 + Bug Hunt → 推送 Draft PR（不合并）。
> 用户已确认口径：资产「尽可能多」；许可证「沿用先例 + Draft 审查」。遵守 AGENTS.md。

## 1. 多模态生成（总控亲自执行）

用 WorkBuddy 内置 ImageGen（文生图，high）生成 6 张明代水墨风 UI 美术资产，统一约束「无文字/字符」规避伪字形：
endgame-review、guide-banner、study-desk-dusk、minister-portrait、ink-frame-cartouche、memorial-open-scroll。

后处理：确定性 Pillow 裁剪（右 16%+下 11%）移除 ImageGen 强制「AI生成 WORKBUDDY」署名水印；原图留存 `artifacts/ui-art-staging/`（gitignored）备查；裁剪脚本 `crop_watermark.py`。

## 2. 工作树与派发

| 角色 | 工作树/分支 | 资产目录 | 集成目标 |
| --- | --- | --- | --- |
| 子代理 A | `_worktrees/ui-art-panels` [ui-art-panels] | `art/panels/` | EndgameReportPanel.cs、GuidePanel.cs |
| 子代理 B | `_worktrees/ui-art-mainui` [ui-art-mainui] | `art/mainui/` | MainUi.cs |

来源文档（许可证红线）由总控亲自编写，诚实披露 WorkBuddy ImageGen 与水印裁剪，标注许可证待所有者确认。
两路文件不相交（A 两个面板脚本 / B 仅 MainUi.cs），均未触碰 asset-ledger.json。

## 3. Independent Review（SHA 绑定，亲自核验非采信自述）

### A 路 — head SHA `4c0631513c3e5af195c881ca9fc1312aac485c11`
- 边界：`git diff --name-only` 仅 GuidePanel.cs、EndgameReportPanel.cs、新增 tests/ui_art_panels_capture.gd(+.uid)、3 资产、PROVENANCE.md；未碰 MainUi.cs/ledger。✅
- 构建：`dotnet build Ming.Godot.csproj` 0 错误。✅
- 验收（总控独立复跑）：`guide_acceptance` PASS、`m3_decree_endgame_acceptance` PASS。✅
- 截图证据：panels-guide.png / panels-endgame.png（1600×960 真实渲染），引导面板完整在视口内、肖像+横幅集成正常。
- 代码审查：纯展示、GD.Load null 兜底、MouseFilter=Ignore、无 GameTime/WorldState 写入、无命令。
- **范围扩大核查**：A 移除了 GuidePanel 的 `SetAnchorsAndOffsetsPreset(Center)`。总控核算：原 Center 锚点(0.5)+Position(420,200) 会把 760×560 面板定位到 (1220,680)，大部分溢出 1600×960 视口——**确属真实既有 bug**；且 guide_acceptance 只断言文案/可见性不断言位置，故此前未暴露。修复后面板在视口内（截图证实），验收无回归。判定合理且安全。✅
- 结论：**PASS**，无 P0/P1。

### B 路 — head SHA `a11c06fb9713f93b9ceda0a40d2c81a042e6ea6c`
- 边界：仅 MainUi.cs、3 资产、PROVENANCE.md；未碰面板/ledger。✅
- 构建：0 错误。✅
- 验收（总控独立复跑）：`ui_headless_acceptance` PASS、`main_ui_transition_acceptance` PASS、`map_input_acceptance` PASS。其中「点击桌面舆图进入战略地图」「转场完成」「收卷返回」均过，且断言「MainUi 只读 ReadModel 与命令门面，不触达可写权威状态」。✅
- 截图证据：mainui-desk.png / mainui-memorial.png（1600×960），黄昏主背景生效、水墨边框与奏疏插画集成正常、无双重地图。
- 代码审查：背景 `?? 原图` 回退；DeskMapPreview.Visible=false 有注释（避免双重地图）且保留 DeskMapScroll 点击区；边框/插画 null 兜底；无状态写入。✅
- 结论：**PASS**，无 P0/P1。

## 4. Bug Hunt

- A：布局修复经独立核算+验收双重验证；`.gd.uid` 提交符合项目入库惯例（tests/ 下 8 个已跟踪）；两工作树未误提交 Godot 生成的 `.cs.uid`（与 main 一致）。判 **BUG_HUNT_CLEAR**。
- B：DeskMapPreview 隐藏后交互保留（验收证实）；背景回退正确；边框/插画不遮挡交互。判 **BUG_HUNT_CLEAR**。
- 未发现 P0/P1。

## 5. UI 红线核对（AGENTS.md §仓库特定必查）

- 真实运行画面：✅ 4 张 1600×960 渲染截图（读图核实美术真实集成）。
- 来源：✅ 两个 PROVENANCE.md（工具/prompt/日期/裁剪）。
- 许可证：⚠️ 沿用 PROJECT_GENERATED 先例，但诚实披露 WorkBuddy ImageGen 与 OpenAI 条款差异、强制水印已裁剪；**待所有者确认**（本 Draft PR 即检查点）。
- FACT/DESIGN/OPEN 语义：✅ 来源文档使用； dusk 背景淡伪字形记 P2。

## 6. P2 债务

- UI-1：许可证/AI 标识口径待所有者确认（WorkBuddy ImageGen 条款 + 水印裁剪是否满足标识办法）。
- UI-2：dusk 背景画中地图残留极淡示意标注（伪字形倾向），居中无法角裁剪；可 inpainting 或换图。
- UI-3：「东亚舆图」标签浮于笔洗上方（排版微调）。
- UI-4：水墨边框浅底与黄昏深色调对比略跳（可换透明描边版）。
- UI-5：裁剪脚本未并入 tools/ui/ming_ui_assets.py 幂等管线。
- UI-6：候选资产未入 asset-ledger.json，待确认后再登记。
- UI-7：截图 CJK 字体局部残缺（MSDF+gl_compatibility 首帧既有现象，非本次引入）。

## 7. 处置

推送 `ui-art-panels`、`ui-art-mainui` 两分支，创建对应 Draft PR。**严格不合并**。许可证/标识确认与账本登记留待所有者审查后决定。

## 8. 高危指令审查

- 未删除任何文件/分支/工作树；未 reset/force-push/改写历史；未触碰主仓库未提交改动。
- 新增资产/文档/接线均为增量；ImageGen 生成与水印裁剪已如实披露。
