# 当前工程现状、差距与实施顺序

> 审计日期：2026-08-13。  
> 本文只说明当前证据，不把设计目标伪装成已实现功能。

## 1. 当前可验证结论

### 已验证

- 本机 .NET SDK：`10.0.400`，位于外置工具链；
- `dotnet build MyGame.sln --nologo` 成功：0 警告、0 错误；
- `dotnet run --project tests/Ming.SmokeTests/Ming.SmokeTests.csproj --no-build` 输出“冒烟测试全部通过”；
- Godot：`4.6.stable.mono.official.89cea1439`；
- 使用该 Godot 的 console 版本曾以 headless editor 方式成功加载**静态 Godot 工程**；地图交互阶段已新增 Godot C# 项目，最终结论以新的构建、headless 交互测试与运行截图为准；
- 现有 8 个 `src` 下的 .NET `.csproj`/模块项目（包含 `Ming.Godot`）已列入解决方案；测试另有 1 个 `.csproj`；
- 现有地图拓扑、规则随机、回合结算、实时行军原型、权限和内存快照已有代码。

### 尚未验证为生产能力

- Godot UI 与 Application/Simulation 的真实命令/查询接入；
- SQLite WAL 持久化和真实 ACID 提交；
- 实时 Scheduler 的完整序列化、重放与快照恢复；
- LLM Provider、Context Compiler 的有限认知和安全密钥路径；
- 90 日“宁远急饷”可玩闭环；
- 完整的资源流、后勤风险、文书和人物机构协作；
- 真实 UI 试玩、存档故障注入、90 日多种子/一年合成长跑。

## 2. 现有结构与目标结构对照

| 当前项目/文件 | 当前证据 | 目标中的位置 | 结论 |
| --- | --- | --- | --- |
| `Ming.Domain/WorldState.cs` | 世界、时间、人物、机构、经济、工业、军事容器 | 权威世界 | 保留思想；补 WorldVersion/随机/Scheduler 等，避免上帝对象 |
| `Domain/Intents/WorldIntent.cs` | 建厂、改编、训练的回合意图 | 结构化 Intent | 概念保留；迁移到实时版本/期限语义 |
| `Simulation/SimulationKernel.cs` | 冻结副本、权限、规则、不变量、回合提交 | 短批次结算 | 保留规则与临时工作区；不再作为主时间模型 |
| `Simulation/RealtimeSimulationRuntime.cs` | 时钟、PriorityQueue、行军到达、日心跳 | 目标实时运行时雏形 | 作为统一方向；需要拆分职责、持久化、确定性和 Commit |
| `Application/TurnOrchestrator.cs` | 内存存储、快照、审计的回合流程 | Commit 工作流 | 原子思想保留；名称和边界并入实时提交 |
| `Application/Ports/WorldPorts.cs` | 世界/审计/快照三个端口 | 过渡外围持久化端口 | `ICommitStore` 目标归 Simulation；Persistence 改为实现它，避免三步非原子 |
| `Agents/AgentRuntime.cs` | 有限字段上下文、规则代理排序 | Agent 雏形 | 保留边界；改成 Perception/Belief、异步期限和 Utility/模型双路 |
| `Persistence/InMemory/*` | 内存世界、Journal、快照 | 测试适配器 | 保留作测试；不能作为正式存档 |
| `Godot/ui_preview.tscn` | C# 交互地图/UI 原型，读取自动生成清单 | 表现层 | 可作为地图交互验证；仍未接入权威 ReadModel/Command 和完整玩法 |
| `content/ming_1627/world.json` | 最小场景与地图内容 | 内容数据 | 保留到年份 ADR/迁移任务；不要现在直接改名 |
| `Ming.SmokeTests` | 无第三方包的行为冒烟 | 回归基础 | 当前通过；扩张后补标准测试项目和故障/重放样本 |

## 3. 最大架构冲突：两套世界推进

目前并存：

```text
旧路径：TurnNumber → ResolveTurn → AdvanceTurn
新路径：CurrentTime → RealtimeSimulationRuntime.Advance → Scheduled Events
```

原策划最终明确采用可暂停实时，因此不能继续向两条路径同时加玩法。

### 裁决

- `GameTime + Scheduler + 单写者` 成为唯一主运行路径；
- `WorldVersion` 取代 `TurnNumber` 的并发/过期语义；
- 旧 `TurnNumber` 暂时只用于读取旧内容/测试，设置明确删除里程碑；
- `ResolveTurn` 中的临时工作区、不变量和“全有或全无”迁入命令/到期批次 Commit；
- Industry 的“每回合生产一次”以后改为月度/排程结算；
- 不立即删除旧实现，先用特征测试锁住已有行为，再渐进迁移，最后删除。

## 4. 当前快照/提交的关键缺口

现有 `TurnOrchestrator` 顺序是：

```text
WorldStore.Commit
→ AuditJournal.Append
→ SnapshotStore.Promote
```

内存适配器中能演示流程，但真实崩溃时三步不是同一事务。目标必须改为：

```text
SQLite 一个事务：状态 + 外部 InputJournal（若有）/ActionOutcome + EventJournal + Commit
→ 成功后发布内存版本
→ 周期 Snapshot 另走“写临时→真实加载→验证→切指针”
```

Snapshot 不属于每条命令的同步事务；Current State、对应的外部 InputJournal 或 ActionOutcome、EventJournal 与 Commit 属于同一正式事务。空小时可跳过，但权威时间实际推进到动作/暂停/目标点时必须提交时间状态。

当前依赖也尚未达到目标图：`Ming.Persistence` 仍引用 Application/Domain，没有引用 Simulation；`Ming.Agents` 仍只引用 Domain。实现阶段要移动/新增端口并更新 `.csproj` 引用，而不是只改文档上的箭头。

## 5. 当前状态哈希缺口

已有 `StateHasher` 是好雏形：对地图、库存、军队、设施、机构、权限和人物排序哈希。

目标还需包括：

- `CurrentTime`；
- `WorldVersion/CommitId`；
- Scheduler 队列和稳定序号；
- 随机流状态；
- Decree/Document/Shipment；
- 人物关系、目标、记忆/Belief 中会影响未来结果的部分；
- 动态地图控制/路线状态；
- 内容/规则版本。

当前哈希不包含 `CurrentTime`，所以两个不同日期但其他字段相同的世界可能得到相同哈希；在正式重放前必须修正。

## 6. 现有实时运行时的优点与风险

### 已有优点

- `PriorityQueue`；
- `DueAt + sequence` 稳定优先级；
- 暂停和速度；
- 精确到达动作；
- UI 外的纯 C#；
- 未知调度事件不直接改状态。

### 需要改进

- 当前 Runtime 直接持有并修改 `State`，尚无统一 Command/Commit Store；
- `Advance(realElapsed)` 仍接收真实时间换算，测试核心应优先 `AdvanceTo(GameTime)`；
- 每小时 `while` 在完整世界规模需压测/可跳转优化；
- Scheduler 队列未进入存档/哈希；
- 日心跳和军队抵达逻辑塞在同一 300 行类，新增后会膨胀；
- 命令类型与旧 `WorldIntent` 分成两套；
- 事件处理后没有完整不变量与数据库事务；
- 军队在移动期间没有明确 `MovementState`，到达前是否可重复命令需定义；
- `DateTime` 当前可用，但长期应封装游戏日历/文化无关序列化。

不要立刻重写。先写覆盖现有行为的确定性测试，再沿黄金用例拆最小接口。

## 7. 代码规模信号

当前核心手写 C# 文件大多在 10–200 行，两个热点：

- `RealtimeSimulationRuntime.cs`：约 309 行；
- `SimulationKernel.cs`：约 276 行。

这不是单凭行数就必须拆，但两者都是“每加一种命令就增长一个分支”的变化热点。统一实时路径时建议：

```text
RealtimeSimulationRuntime
→ 只保留时钟、队列、顺序和调用规则

每个真实业务系统
→ 用小型具体规则处理自己的命令/到期动作
```

不要把每个动作拆成 18 个类；先按 Logistics、Military、Time 三个真实职责分开即可。

## 8. 文档现状

已有：

- `README.md`：已更新为蓝图入口，并明确旧回合骨架与实时目标；
- `docs/建模与框架说明.md`：对象、权限和临时工作区；
- `docs/开源架构考古与复用边界.md`：外部参考审计；
- `docs/UI资源与使用说明.md`：视觉资源、错误现代地图的撤下状态与当前交互原型。

仍需注意：

- 既有文档不能删除其历史价值，根 README 已链接本设计蓝图并标注旧回合原型状态；
- 可交互地图原型不等于真实 ReadModel/Command 接入；正式玩法仍须经过 Application 与 Simulation。

本轮遵守“先画图纸”：只新增/修订设计文档和根 README 导航，不修改玩法代码。

## 9. 建议实施序列（精确到可执行任务）

### A. M0 收尾：导航和基线

1. 根 README 指向蓝图、更正 SDK 事实、标记旧回合状态：**本轮已完成**；
2. ADR-001 至 ADR-006 正式短文件：**本轮已完成**；
3. `.gitignore` 已存在；实现阶段继续保持 `bin/obj/.godot` 不进入版本管理；
4. 17 号宁远最小 Source Ledger 骨架已建立；下一步补外部证据，并先对场景控制表做纸面/数值试玩；
5. M0 评审通过后才冻结基线。

### B. 特征测试锁住现有行为

1. 实时行军相邻检查；
2. 同时刻事件稳定顺序；
3. 暂停与速度行为；
4. 旧建厂/改编/训练的守恒；
5. 当前场景 Loader 和地图哈希；
6. 重复命令和移动中重复命令现状。

### C. 统一身份与时间

1. 新建强类型 `WorldVersion`、`CommitId`、`GameTime`；
2. 命令 Envelope 使用 `ExpectedWorldVersion`；
3. Runtime 提供 `AdvanceTo(GameTime)`；
4. Scheduler 项显式版本化、可序列化；
5. 旧 `TurnNumber` 进入兼容适配器；
6. 哈希加入时间、Scheduler 和随机状态。

### D. 打通黄金运输用例（先内存）

1. 只增加 `grain`、两个 Stockpile；
2. 建最小 Route/Capacity；
3. 定义 `CreateShipmentCommand`；
4. 权限/库存/容量检查；
5. 出发 Delta；
6. `ShipmentArrival` 到期动作；
7. 损耗和目的入库；
8. 守恒、幂等、Replay 测试。

### E. 把内存 Commit 替换为 SQLite

1. 只引入 `Microsoft.Data.Sqlite`；
2. 设计最小 schema：meta、current state、scheduled actions、journal、commands、commits；
3. 一条事务提交；
4. 崩溃故障注入；
5. 加载/重放；
6. READY Snapshot 和回退；
7. 迁移框架只支持真实 V1→V2 需求，不预造十个版本。

### F. 接 Godot

1. 定义 `MapReadModel/InboxReadModel/ShipmentReadModel`；
2. Presenter；
3. 6 节点规则图覆盖当前视觉底图；
4. 暂停/速度调用 Application；
5. 政令确认；
6. 粮队插值；
7. 失败解释；
8. 真实运行截图/试玩证据。

### G. 人物机构和 AI

1. Appointment/Capability/文书；
2. Utility AI；
3. Belief/三种报告；
4. DecisionRequest 版本/期限；
5. fake provider 测试；
6. 真实 Provider、预算和密钥；
7. 最多 3 人的模型增强。

## 10. 暂不改代码的原因

当前用户明确要求“先弄文档、先把图纸规划好”。因此本轮：

- 不迁移 `TurnNumber`；
- 不引入 SQLite 包；
- 不重构 Runtime；
- 不重命名 `content/ming_1627`；
- 不删除现有回合路径；
- 不接模型；
- 不改 UI。

这样避免设计尚未审阅就造成大范围不可逆实现偏差。下一阶段应从 A/B 开始，而不是直接做全国地图或 LLM。

## 11. 下一阶段的第一个验收包

建议下一次只完成：

```text
补齐宁远最小 Source Ledger 的外部证据
+ 场景控制表纸面/数值试玩记录
+ 现有行为特征测试
+ GameTime/WorldVersion/CommitId 设计落地
+ StateHasher 覆盖时间/Scheduler/随机
```

验收证据：

- `dotnet build`；
- 全部现有/新增测试；
- 同一场景重复哈希；
- 旧场景仍可加载；
- 文档中的旧/新路径状态准确。

之后才进入 Shipment 用例。
